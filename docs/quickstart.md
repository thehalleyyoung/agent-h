# Quickstart

```bash
agent-h init
agent-h doctor
agent-h -p "summarize this repo"
agent-h
```

In the REPL, use `/help`, `/cost`, `/diff`, `/exit`, and `/skill save <name>`. Ctrl-D exits and saves; Ctrl-C interrupts the current step while keeping the session.
